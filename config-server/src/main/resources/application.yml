server:
  port: 8098

eureka:
  client:
    service-url:
      defaultZone: https://admintareas-production.up.railway.app/

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Jmrj24/admin_tareas.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}


